import NewsCard from "./NewsCard";
const NewsCardsSection = () => {
  return (
    <section>
      <h1 className="uppercase font-medium text-5xl text-white py-10">News</h1>
      <NewsCard />
    </section>
  );
};

export default NewsCardsSection;
